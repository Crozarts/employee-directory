import React from 'react';
import './style.css';

function Header() {
    return (
        <div>
            <header>
                <h1>Employee Directory</h1>
                <p>Click on the button to sort by name or use the search box below to narrow your search results.</p>
            </header>
        </div>
    );
}

export default Header;